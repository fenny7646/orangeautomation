[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "==3.1.3"
flask-cors = "==6.0.5"
python-dotenv = "==1.2.3"
werkzeug = "==3.1.8"
jinja2 = "==3.1.6"
playwright = ">=1.50.0"

[dev-packages]
pytest = ">=8.0.0"
pytest-playwright = ">=0.7.0"

[requires]
python_version = "3.13"

[scripts]
start = "python run.py"
dev = "python run.py"
test = "pytest test/test_orangehrm.py -v"

